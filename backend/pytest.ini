[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning

markers =
    security: Security and penetration tests
    concurrent: Race condition and concurrency tests
    slow: Tests that take longer to execute
    admin: Super admin endpoint tests
